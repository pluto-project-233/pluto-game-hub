# Database (Supabase PostgreSQL - Production)
# Using Supabase production database for all environments
# Note: Special characters in password must be URL-encoded (# becomes %23)
DATABASE_URL="postgresql://postgres.ygmeectixgbbisbbpolv:Ace2012seta%23@aws-1-ap-south-1.pooler.supabase.com:6543/postgres"

# Firebase (Service Account JSON string)
# You can get this from Firebase Console > Project Settings > Service Accounts
FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", ...}'

# Server
PORT=3000
NODE_ENV=development

# JWT Secret (for execution tokens)
JWT_SECRET="your-jwt-secret-here"

# HMAC Secret (for game backend signatures)
HMAC_SECRET="your-hmac-secret-here"


